import { Link } from 'react-router-dom';
const plans = [
  { name: 'Starter', audience: 'Para validar CISPAR en un entorno inicial.', items: ['Instalación guiada', 'Operación local con Docker', 'Acceso a releases publicadas'], action: 'Solicitar acceso' },
  { name: 'Enterprise', audience: 'Para equipos con mayor volumen, integración o requisitos de despliegue.', items: ['Planeación de despliegue', 'Alcance definido por entorno', 'Acompañamiento comercial'], action: 'Hablar con ventas', featured: true },
];
export function PricingPage(): React.ReactElement { return <section className="page"><div className="section-container"><div className="page-heading"><p className="eyebrow">PLANES</p><h1>Elige el alcance adecuado para tu operación.</h1><p>El precio y los límites se definen según el entorno, número de fuentes y requisitos de despliegue. No publicamos cifras que todavía no correspondan al servicio entregado.</p></div><div className="plan-grid">{plans.map((plan) => <article className={`plan-card ${plan.featured ? 'featured-plan' : ''}`} key={plan.name}><p className="eyebrow">{plan.name}</p><h2>{plan.audience}</h2><p className="plan-price">Propuesta a medida</p><ul>{plan.items.map((item) => <li key={item}>{item}</li>)}</ul><Link className={plan.featured ? 'btn-primary' : 'btn-secondary'} to="/contact">{plan.action}</Link></article>)}</div><p className="pricing-note">Para soporte de una plataforma específica, integraciones o un despliegue con varias sedes, cuéntanos sobre tu entorno.</p></div></section>; }
